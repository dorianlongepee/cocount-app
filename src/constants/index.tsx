export const baseUrl = "http://localhost:8080/api/v1";

export enum snackType {
  SUCCESS = "success",
  INFO = "info",
  WARNING = "warning",
  ERROR = "error",
}

export enum tabChoice {
  EXPENSES = "EXPENSES",
  BALANCE = "BALANCE",
  SETTINGS = "SETTINGS",
}
